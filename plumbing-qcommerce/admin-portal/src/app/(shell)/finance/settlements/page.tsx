"use client";
import{useEffect,useState}from"react";import StatusBadge from"@/components/admin-shell/StatusBadge";import{listStoreSettlements,type StoreSettlementSummary}from"@/services/financeService";import{currency,financeTone,formatDate}from"../financeHelpers";
export default function StoreSettlementsPage(){const[rows,setRows]=useState<StoreSettlementSummary[]>([]);const[status,setStatus]=useState("");useEffect(()=>{listStoreSettlements({status,size:50}).then(r=>setRows(r.content))},[status]);return <section className="panel"><div className="page-header"><div><h1 className="page-title">Store Settlements</h1><p className="page-subtitle">Gross, commission, and net payable by store.</p></div><select className="field-input" value={status} onChange={e=>setStatus(e.target.value)}><option value="">All</option><option>PENDING</option><option>PAID</option><option>FAILED</option></select></div><div className="table-shell"><table className="table"><thead><tr><th>Settlement</th><th>Store</th><th>Gross</th><th>Commission</th><th>Net</th><th>Status</th><th>Paid</th></tr></thead><tbody>{rows.map(x=><tr key={x.settlementId}><td>#{x.settlementId}</td><td>{x.storeName}</td><td>{currency(x.grossAmount)}</td><td>{currency(x.commissionAmount)}</td><td>{currency(x.netAmount)}</td><td><StatusBadge tone={financeTone(x.status)}>{x.status}</StatusBadge></td><td>{formatDate(x.paidAt)}</td></tr>)}</tbody></table></div></section>}