export function currency(value?: number | null) { return new Intl.NumberFormat("en-IN", { style: "currency", currency: "INR", maximumFractionDigits: 0 }).format(value ?? 0); }
export function formatDate(value?: string | null) { if (!value) return "-"; const parsed = new Date(value); return Number.isNaN(parsed.getTime()) ? value : parsed.toLocaleString("en-IN"); }
export function financeTone(status?: string | null): "success" | "warning" | "danger" | "neutral" | "info" { if (!status) return "neutral"; if (["SUCCESS", "PAID", "APPROVED", "PROCESSED"].includes(status)) return "success"; if (["FAILED", "REJECTED"].includes(status)) return "danger"; if (["PROCESSING"].includes(status)) return "info"; if (["REFUNDED"].includes(status)) return "info"; if (["PENDING"].includes(status)) return "warning"; return "neutral"; }