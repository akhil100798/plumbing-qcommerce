"use client"; import Link from "next/link"; import {useEffect,useState} from "react"; import StatusBadge from "@/components/admin-shell/StatusBadge"; import {pendingKyc,type KycSummary} from "@/services/plumberManagerService"; import {date,tone} from "../plumberHelpers";
export default function Page(){const[rows,setRows]=useState<KycSummary[]>([]),[error,setError]=useState("");useEffect(()=>{pendingKyc().then(setRows).catch(e=>setError(e.message))},[]);return <section className="panel"><div className="page-header"><div><h1 className="page-title">KYC Approvals</h1><p className="page-subtitle">Review masked identity and banking documents.</p></div></div>{error?<div className="field-error">{error}</div>:null}<div className="table-wrap"><table className="data-table"><thead><tr><th>Plumber</th><th>Phone</th><th>Experience</th><th>Service Areas</th><th>Submitted</th><th>Status</th><th></th></tr></thead><tbody>{rows.map(k=><tr key={k.kycId}><td>{k.plumberName}</td><td>{k.phone}</td><td>{k.experienceYears??" - "} years</td><td>{k.serviceAreas||" - "}</td><td>{date(k.submittedAt)}</td><td><StatusBadge tone={tone(k.status)}>{k.status}</StatusBadge></td><td><Link className="inline-link" href={`/plumber-manager/kyc/${k.kycId}`}>Review</Link></td></tr>)}</tbody></table></div></section>}
