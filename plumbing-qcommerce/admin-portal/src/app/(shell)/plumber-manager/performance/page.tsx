"use client"; import Link from "next/link"; import {useEffect,useState} from "react"; import {getPerformance,listPlumbers,type Performance,type PlumberSummary} from "@/services/plumberManagerService"; import {money} from "../plumberHelpers";
type Row={plumber:PlumberSummary;performance:Performance}; export default function Page(){const[rows,setRows]=useState<Row[]>([]),[error,setError]=useState("");useEffect(()=>{listPlumbers({size:100}).then(async p=>setRows(await Promise.all(p.content.map(async plumber=>({plumber,performance:await getPerformance(plumber.plumberId)}))))).catch(e=>setError(e.message))},[]);return <section className="panel"><div className="page-header"><div><h1 className="page-title">Plumber Performance</h1><p className="page-subtitle">Workload, outcomes, ratings, and earnings by technician.</p></div></div>{error?<div className="field-error">{error}</div>:null}<div className="table-wrap"><table className="data-table"><thead><tr><th>Plumber</th><th>Completed</th><th>Active</th><th>Cancelled</th><th>Rating</th><th>Total Earnings</th><th>Monthly</th></tr></thead><tbody>{rows.map(({plumber:p,performance:x})=><tr key={p.plumberId}><td><Link className="inline-link" href={`/plumber-manager/plumbers/${p.plumberId}`}>{p.fullName}</Link></td><td>{x.completedJobs}</td><td>{x.activeJobs}</td><td>{x.cancelledJobs}</td><td>{x.averageRating??" - "}</td><td>{money(x.totalEarnings)}</td><td>{money(x.monthlyEarnings)}</td></tr>)}</tbody></table></div></section>}
