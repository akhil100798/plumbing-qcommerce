"use client";import{useEffect,useState}from"react";import StatCard from"@/components/admin-shell/StatCard";import{segments}from"@/services/marketingService";export default function Page(){const[rows,setRows]=useState<{segment:string;description:string;estimatedUserCount:number}[]>([]);useEffect(()=>{segments().then(setRows)},[]);return <section className="panel"><h1 className="page-title">User Segments</h1><div className="stat-grid">{rows.map(s=><StatCard key={s.segment} label={s.segment.replaceAll("_"," ")} value={s.estimatedUserCount}/>)}</div></section>}
