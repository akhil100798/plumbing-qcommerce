module.exports=async c=>{const p=c.pages.plumber;await p.getByRole('button',{name:'Start Navigation',exact:true}).click();await p.getByRole('button',{name:'Confirm arrival at customer'}).click();await p.waitForTimeout(3500);await c.snap('plumber','arrived');const r=await c.api('GET','/api/v1/orders/'+c.order.id,'CUSTOMER');c.save('logs/workflow-arrival-continuation.json',{status:r.status,id:r.data?.id,orderStatus:r.data?.status,arrivedAt:r.data?.arrivedAt});c.flush();};
